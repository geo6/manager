{
    "dependencies": {
        "@babel/runtime": "^7.4.5",
        "ol": "^5.3.3",
        "sidebar-v2": "^0.4.0"
    },
    "devDependencies": {
        "@babel/core": "^7.4.5",
        "@babel/plugin-transform-runtime": "^7.4.4",
        "concurrently": "^4.1.1",
        "eslint": "^6.0.1",
        "eslint-config-standard": "^12.0.0",
        "eslint-plugin-import": "^2.18.0",
        "eslint-plugin-node": "^9.1.0",
        "eslint-plugin-promise": "^4.2.1",
        "eslint-plugin-standard": "^4.0.0",
        "parcel-bundler": "^1.12.3",
        "sass": "^1.22.1"
    },
    "scripts": {
        "postinstall": "npm run build",
        "test": "eslint resources/javascript/",
        "fix": "eslint resources/javascript/ --fix",
        "watch": "concurrently --raw \"npm:watch:*\"",
        "watch:map": "parcel watch --out-dir=public/ --out-file=map.js resources/javascript/map.js --no-cache --no-hmr",
        "watch:table": "parcel watch --out-dir=public/ --out-file=table.js resources/javascript/table.js --no-cache --no-hmr",
        "build": "concurrently --raw \"npm:build:*\"",
        "build:map": "parcel build --out-dir=public/ --out-file=map.js --no-cache --no-source-maps resources/javascript/map.js",
        "build:table": "parcel build --out-dir=public/ --out-file=table.js --no-cache --no-source-maps resources/javascript/table.js"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}
