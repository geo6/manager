<tbody>
    {% for record in records %}
    {% set id = record[table.name ~ '_' ~ primaryKey] %}
    <tr data-id="{{ id }}">

        {% for column in table.columns %}
            {% if column.name in file %}
            {{ thumbnail(id, column.name, record[table.name ~ '_' ~ column.name]) }}
            {% else %}
            {{ value(record[table.name ~ '_' ~ column.name], column.type) }}
            {% endif %}
        {% endfor %}

        {% for fk in foreignKeys %}
        {% for column in fk.foreignTable.columns %}
        {{ value(record[fk.foreignTable.name ~ '_' ~ column.name], column.type) }}
        {% endfor %}
        {% endfor %}

    </tr>
    {% endfor %}
</tbody>